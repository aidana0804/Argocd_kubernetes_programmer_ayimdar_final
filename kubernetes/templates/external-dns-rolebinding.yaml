apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: external-dns
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: external-dns-view
subjects:
- kind: ServiceAccount
  name: {{ .Values.externalDns.serviceAccount.name }}
  namespace: default
